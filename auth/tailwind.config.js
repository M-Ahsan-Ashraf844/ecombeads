/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./authapp/templates/**/*.html", // all templates in your app
    "./templates/**/*.html", // project-level templates (if any)
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
